Il progetto è centrato sull'allenamento di un deep learning model per performate image classification su di un dataset caratterizato da un domain shift tra training e test data.

è possibile inserire il proprio dataset ed allenare la rete per conto proprio.
